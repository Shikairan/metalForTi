# symbolTorch — 需 Python >= 3.11（torch-symbolic）
#
# 推荐：使用仓库根目录统一安装（Python 3.11 + torch 2.10 cu130）：
#   见 ../README.md → pip install -r ../requirements.txt
#
# 单独安装本目录时，请先按 README 装好 GPU 版 torch，再 pip install -r requirements.txt
# 首次运行 PySR 可能自动安装 Julia。

numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
torch>=2.0.0
torch-geometric>=2.7.0
torch-symbolic>=1.0.1
scikit-learn>=1.3.0
