# metalForTi — 主环境依赖（gnnDir 训练 + grd 梯度反推）
#
# Python 版本：3.10（推荐，与下列 torch 版本配套测试）
#   conda create -n metal4ti python=3.10 -y && conda activate metal4ti
#
# 安装顺序（GPU / CUDA 11.8 示例）：
#   pip install torch==2.0.1 --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
#
# CPU only：
#   pip install torch==2.0.1
#   pip install -r requirements.txt
#
# 符号蒸馏 symbolTorch 需 Python >= 3.11，请另建环境：
#   pip install -r symbolTorch/requirements.txt
#
# 注意：
# - 保持 numpy<2（与 torch 2.0.x 兼容）
# - 运行 grd 时请在仓库根目录：export PYTHONPATH=$(pwd)

numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
torch==2.0.1
torch-geometric==2.7.0

# grd 可选：GNNInversionBenchmark 可视化（非 run_inversion 必需）
matplotlib>=3.7.0
