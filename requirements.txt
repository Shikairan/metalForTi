# metalForTi — 统一环境依赖（gnnDir + grd + symbolTorch）
#
# Python：3.11（推荐，全模块共用）
#   conda create -n metal4ti python=3.11 -y && conda activate metal4ti
#
# GPU（驱动 ≥590，nvidia-smi 显示 CUDA 13.1）— 必须先装 PyTorch cu130，再装本文件：
#   pip install torch==2.10.0 torchvision==0.25.0 torchaudio==2.10.0 \
#     --index-url https://download.pytorch.org/whl/cu130
#   pip install pyg_lib torch_scatter torch_sparse torch_cluster torch_spline_conv \
#     -f https://data.pyg.org/whl/torch-2.10.0+cu130.html
#   pip install -r requirements.txt
#
# CPU only：
#   pip install torch==2.10.0 torchvision==0.25.0 torchaudio==2.10.0 \
#     --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# 说明：
# - nvidia-smi 中的「CUDA Version: 13.1」是驱动支持的上限，无需本机单独装 CUDA Toolkit
# - PyTorch cu130 wheel 自带 CUDA 运行时，与 Driver 590 兼容
# - 勿在本文件中写 torch==...，避免 pip 从 PyPI 覆盖已安装的 GPU 版 torch
# - 运行 grd：在仓库根目录 export PYTHONPATH=$(pwd)

numpy>=1.26.0,<3.0.0
pandas>=2.0.0,<3.0.0
torch-geometric==2.8.0
matplotlib>=3.7.0
scikit-learn>=1.3.0

# symbolTorch（需 Python >= 3.11，与上列共用同一环境）
torch-symbolic>=1.0.1
