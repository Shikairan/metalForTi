# gnnDir 历史依赖栈（torch 2.0.1 + cu118 + Python 3.10）
#
# 新环境请改用仓库根目录 requirements.txt + README.md：
#   Python 3.11 + torch 2.10.0 cu130（适配 Driver 590 / CUDA 13.1）
#
# 以下为旧栈，仅在做版本回退时参考：
#   conda create -n metalgnn-legacy python=3.10 -y
#   pip install torch==2.0.1 --index-url https://download.pytorch.org/whl/cu118
#   pip install -r gnnDir/requirements-legacy.txt

# 见 requirements-legacy.txt（本文件仅作跳转说明）
